---
title: C6273 | Документация Майкрософт
ms.date: 11/15/2016
ms.prod: visual-studio-dev14
ms.technology: vs-ide-code-analysis
ms.topic: reference
f1_keywords:
- C6273
helpviewer_keywords:
- C6273
ms.assetid: e780e504-8b8d-4d61-b15f-4859133134ad
caps.latest.revision: 15
author: corob-msft
ms.author: corob
manager: jillfra
ms.openlocfilehash: f0bc2215722a91137b6770c816021123378317b6
ms.sourcegitcommit: 6cfffa72af599a9d667249caaaa411bb28ea69fd
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 09/02/2020
ms.locfileid: "77278507"
---
# <a name="c6273"></a>C6273
[!INCLUDE[vs2017banner](../includes/vs2017banner.md)]

Предупреждение 6273-нецелочисленное значение, передаваемое в качестве параметра, \<number> Если требуется целое число при вызове \<function> : при передаче значения указателя следует использовать% p  
  
 Это предупреждение означает, что строка формата задает целое число, например, определенную `%d` длину или спецификацию приоритета для, `printf` а не целое число, например `float` , строка или `struct` передается как параметр. Эта ошибка, скорее всего, приведет к неверным результатам.  
  
## <a name="example"></a>Пример  
 В следующем коде создается это предупреждение, поскольку вместо функции to требуется целое `float` число `sprintf` :  
  
```  
#include <stdio.h>  
#include <string.h>  
  
void f_defective()  
{  
  char buff[50];  
  float f=1.5;  
  
  sprintf(buff, "%d",f);  
}  
```  
  
 В следующем коде используется целочисленное приведение для исправления этого предупреждения:  
  
```  
#include <stdio.h>  
#include <string.h>  
  
void f_corrected()  
{  
  char buff[50];  
  float f=1.5;  
  
  sprintf(buff,"%d",(int)f);  
}  
```  
  
 В следующем коде используется функция безопасной обработки строк, `sprintf_s` для устранения этого предупреждения:  
  
```  
#include <stdio.h>  
#include <string.h>  
  
void f_safe()  
{  
  char buff[50];  
  float f=1.5;  
  
  sprintf_s(buff,50,"%d",(int)f);  
}  
```  
  
 Это предупреждение неприменимо в Windows 9x и Windows NT версии 4, так как на этих платформах не поддерживается% p.  
  
## <a name="see-also"></a>См. также:  
 [sprintf, _sprintf_l, swprintf, _swprintf_l, \_ _swprintf_l](https://msdn.microsoft.com/library/f6efe66f-3563-4c74-9455-5411ed939b81)
