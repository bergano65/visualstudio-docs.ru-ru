---
title: C6269
ms.date: 11/04/2016
ms.topic: reference
f1_keywords:
- C6269
helpviewer_keywords:
- C6269
ms.assetid: a01fa7fa-fc6c-4af7-ac8c-585e44e60cca
author: mikeblome
ms.author: mblome
manager: markl
ms.workload:
- multiple
ms.openlocfilehash: ebb783897117237cd5fb3155060082d1f5a70c8d
ms.sourcegitcommit: 535ef05b1e553f0fc66082cd2e0998817eb2a56a
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 10/07/2019
ms.locfileid: "72013430"
---
# <a name="c6269"></a>C6269

предупреждение C6269: возможно, неверный порядок операций: разыменование пропускается

Это предупреждение означает, что результат разыменования указателя игнорируется, что вызывает вопрос о том, почему на первом месте происходит разыменование указателя.

Компилятор правильно оптимизирует ненужное разыменование. Однако в некоторых случаях этот дефект может отражать приоритет или логическую ошибку.

Одной из распространенных причин этой ошибки является оператор выражения в форме:

```cpp
*p++;
```

Если назначение этой инструкции просто увеличивает указатель `p`, то разыменование не требуется. Однако если цель — увеличить расположение, на которое указывает `p`, программа будет работать не так, как предполагалось, так как конструкция `p++` интерпретируется как `(p++)` вместо `(*p)++`.

## <a name="example"></a>Пример

Это предупреждение создается в следующем коде:

```cpp
#include <windows.h>

void f( int *p )
{
    // code ...
  if( p != NULL )
    *p++;
    // code ...
}
```

Чтобы исправить это предупреждение, используйте круглые скобки, как показано в следующем коде:

```cpp
#include <windows.h>

void f( int *p )
{
    // code ...
  if( p != NULL )
    (*p)++;
    // code ...
}
```